# Visualization

To visualize the model's predictions, run:

1. For training and validation loss:
   ```bash
   python scripts/visualization/plot_loss.py --log_dir <experiment_directory>
   ```
   This will generate a PDF file at `<experiment_directory>/visualization/plot_loss.pdf`.

